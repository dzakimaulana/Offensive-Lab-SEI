<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Favorites</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body class="bg-gray-100">
    <div class="container mx-auto p-4">
      <h1 class="text-3xl font-bold text-center text-gray-800 mb-6">
        Your Favorite Books
      </h1>
      <% if (favorites.length === 0) { %>
      <p class="text-center text-gray-600">
        No favorite books yet. Add some from the dashboard!
      </p>
      <% } else { %>
      <ul class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">
        <% favorites.forEach(favorite => { %>
        <li class="bg-white p-4 rounded-lg shadow-md">
          <h2 class="text-xl font-semibold text-gray-800">
            <%= favorite.title %>
          </h2>
          <p class="text-gray-600">
            <strong>Author:</strong> <%= favorite.author %>
          </p>
          <p class="text-gray-600">
            <strong>Year:</strong> <%= favorite.year %>
          </p>
          <p class="text-gray-700 mt-2"><%= favorite.description %></p>
          <form action="/delete-favorite" method="POST" class="mt-4">
            <input type="hidden" name="bookId" value="<%= favorite.id %>" />
            <button
              type="submit"
              class="w-full bg-red-600 text-white py-2 px-4 rounded-md hover:bg-red-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-red-500"
            >
              Delete
            </button>
          </form>
        </li>
        <% }) %>
      </ul>
      <% } %>
      <div class="mt-6 text-center">
        <a href="/dashboard" class="text-indigo-600 hover:text-indigo-500"
          >Back to Dashboard</a
        >
        |
        <a href="/logout" class="text-indigo-600 hover:text-indigo-500"
          >Logout</a
        >
      </div>
    </div>
  </body>
</html>
